import 'package:flutter/material.dart';

class LifelineCoinScreen extends StatelessWidget {
  const LifelineCoinScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lifeline Coin'),
      ),
      body: Container(),
    );
  }
}
