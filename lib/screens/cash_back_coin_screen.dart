import 'package:flutter/material.dart';

class CashBackCoinScreen extends StatelessWidget {
  const CashBackCoinScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cash Back Coin'),
      ),
      body: Container(),
    );
  }
}
