import 'package:flutter/material.dart';

class WalletAndCardScreen extends StatelessWidget {
  const WalletAndCardScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Wallet & Card'),
      ),
      body: Container(),
    );
  }
}
